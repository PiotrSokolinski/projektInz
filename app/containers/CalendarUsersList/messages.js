/*
 * CalendarUsersList Messages
 *
 * This contains all the text for the CalendarUsersList container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.containers.CalendarUsersList'

export default defineMessages({
  buttonText: {
    id: `${scope}.buttonText`,
    defaultMessage: 'Save changes',
  },
})
