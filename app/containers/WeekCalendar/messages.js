/*
 * WeekCalendar Messages
 *
 * This contains all the text for the WeekCalendar container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.containers.WeekCalendar'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WeekCalendar container!',
  },
})
